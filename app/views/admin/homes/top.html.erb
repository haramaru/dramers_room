<%= render 'admin/header' %>

<h1>ユーザーレビュー一覧</h1>
<% @reviews.each do |review| %>
  <h5><%= review.user.name %></h5>
  <p><%= review.created_at.strftime('%Y/%m/%d') %></p>
  <p><%= link_to review.review, admin_review_path(review.id) %></p>
<% end %>

<%= render 'admin/footer' %>